// apps/editor — página principal
// O Pascal Editor é montado aqui com a camada RPG sobreposta
import { EditorRoot } from "@/components/editor-root"

export default function EditorPage() {
  return <EditorRoot />
}
